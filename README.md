## TeXnique Bot
This is a Firefox extension for https://texnique.xyz. It will auto-solve all the problems after a timed (or non-timed) game has been started.

To setup the bot, search about:debugging on Firefox, then click 'This Firefox' and 'Load Temporary Add-on...'. Then select the manifest.json.